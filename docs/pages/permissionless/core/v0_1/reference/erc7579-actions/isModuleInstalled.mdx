import VersionWarning from "../../../VersionWarning"

<VersionWarning version="0.1" />

# isModuleInstalled

Checks if an [ERC-7579](https://eips.ethereum.org/EIPS/eip-7579) module is installed on the smart account. Check out [this guide](/permissionless/core/how-to/accounts/use-erc7579-account) for a complete tutorial.

## Usage

::::code-group

```ts [example.ts]
// [!include ~/snippets/v0_1/erc7579/isModuleInstalled.ts:safe-smart-account-client]
```

```ts [publicClient.ts]
// [!include ~/snippets/v0_1/publicClient.ts:client]
```

```ts [signer.ts]
// [!include ~/snippets/v0_1/signer.ts]
```

::::

## Returns

- **Type:** `boolean`

True if the module is installed, false otherwise.

## Parameters

### address

- **Type:** `Address`

Address of the module to install.

### type

- **Type:** `ModuleType`

Type of the module to install. Accepted values are `"validator" | "executor" | "fallback" | "hook"`.

### context

- **Type:** `Hex`

Context bytes that will be passed to the module as part of `additionalContext`.
