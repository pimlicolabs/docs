import VersionWarning from "../../VersionWarning"

<VersionWarning version="0.2" />

# getChainId

`SmartAccountClient` extebds viem's `BundlerClient` and so extends the `getChainId` method.

You can view the full list of options for `getChainId` [at viem](https://viem.sh/account-abstraction/actions/bundler/getChainId).