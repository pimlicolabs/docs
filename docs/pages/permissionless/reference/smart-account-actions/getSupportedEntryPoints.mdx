import VersionWarning from "../../VersionWarning"

<VersionWarning version="0.2" />

# getSupportedEntryPoints

`SmartAccountClient` extebds viem's `BundlerClient` and so extends the `getSupportedEntryPoints` method.

You can view the full list of options for `getSupportedEntryPoints` [at viem](https://viem.sh/account-abstraction/actions/bundler/getSupportedEntryPoints).