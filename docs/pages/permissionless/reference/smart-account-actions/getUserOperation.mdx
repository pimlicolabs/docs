import VersionWarning from "../../VersionWarning"

<VersionWarning version="0.2" />

# getUserOperation

`SmartAccountClient` extebds viem's `BundlerClient` and so extends the `getUserOperation` method.

You can view the full list of options for `getUserOperation` [at viem](https://viem.sh/account-abstraction/actions/bundler/getUserOperation).