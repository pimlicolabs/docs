import VersionWarning from "../../VersionWarning"

<VersionWarning version="0.2" />

# waitForUserOperationReceipt

`SmartAccountClient` extebds viem's `BundlerClient` and so extends the `waitForUserOperationReceipt` method.

You can view the full list of options for `waitForUserOperationReceipt` [at viem](https://viem.sh/account-abstraction/actions/bundler/waitForUserOperationReceipt).