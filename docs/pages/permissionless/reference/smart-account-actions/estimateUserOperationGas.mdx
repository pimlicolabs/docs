import VersionWarning from "../../VersionWarning"

<VersionWarning version="0.2" />

# estimateUserOperationGas

`SmartAccountClient` extends viem's `BundlerClient` and so extends the `estimateUserOperationGas` method.

You can view the full list of options for `estimateUserOperationGas` [at viem](https://viem.sh/account-abstraction/actions/bundler/estimateUserOperationGas).