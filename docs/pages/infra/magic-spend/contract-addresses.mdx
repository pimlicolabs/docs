# MagicSpend++ contract addresses

should we add a small reference page to tell users what the current addresses are, what tokens are enabled, and what chains are enabled, and what limits there currently are for the tokens per user?